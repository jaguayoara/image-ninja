# Carpeta `img/` (raiz)

Esta carpeta recibio el logo original (2816x1536) que se uso para
generar todas las versiones que viven en `assets/` y `static/img/`.

El archivo `ImageNinja.png` (5.3 MB) NO se commitea al repo porque
ya tenemos todas las versiones optimizadas en sus respectivos lugares.

Si necesitas regenerar las versiones:
```
python scripts/make_logos.py
```
